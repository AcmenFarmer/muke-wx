
<view class="container">
  <image bind:tap="onNext" class="icon" src="{{last ? disLeftSrc : leftSrc}}" alt="" />
  <text class="title">{{title}}</text>
  <image bind:tap="onPrev" class="icon" src="{{first ? disRightSrc : rightSrc}}" alt="" />
</view>
