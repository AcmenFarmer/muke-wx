
<view class="container">
  <image bind:tap="onNext" class="icon" src="{{first ? leftSrc : disLeftSrc}}" alt="" />
  <text class="title">{{title}}</text>
  <image bind:tap="onPrev" class="icon" src="{{last ? disRightSrc : rightSrc}}" alt="" />
</view>
